# Python-DB-IP
A simple to use Python library for the DB-IP IP dataset.

This library can import data into PostgreSQL, MySQL and MSSQL. It can be used to import all datasets similar to the php importer tool provided in https://db-ip.com. 

It is still under development.


